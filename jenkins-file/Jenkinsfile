pipeline {
    agent {
        docker { image 'postgres:12.8' }
    }
    stages {
        stage('check-version') {
            steps {
                sh 'psql --version'
            }
        }
    }
}
